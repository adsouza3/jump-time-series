# Getting started

After cloning the repository: 
### `nvm use`
### `yarn install`
### `yarn start`

# Using the app

You can either import a data file or simply generate data in the browser, which is faster.

To zoom in or out, use the buttons in the top right.
To move the selected data, click the time series scrollbar above the displayed data.